node_modules/
npm-debug.log*
.env
.env.local
.DS_Store

# Gegenereerd door `npm run build` (scripts/build-site.js). Draait ook in CI en
# bij npm start, dus de gebouwde site hoort niet in git. Bewerk assets/ en
# content/, nooit public/.
/public/
